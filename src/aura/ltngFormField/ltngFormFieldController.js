/**
 * Created by tarren.anderson on 5/23/17.
 */
({
    doInit: function (component, event, helper) {
        helper.doInit(component, event, helper);
    },
    handleValueChange : function(component, event, helper){
        helper.handleValueChange(component, event, helper);
    },
    handleToggleChange : function(component, event, helper){
        helper.handleToggleChange(component, event, helper);
    },
    handleShowErrors : function(component, event, helper){
        helper.handleShowErrors(component, event, helper);
    },
    handleValidityInit : function(component, event, helper){
        helper.handleValidityInit(component, event, helper);
    },
    handleToggleMultiple : function(component, event, helper){
        helper.handleToggleMultiple(component);
    }
})